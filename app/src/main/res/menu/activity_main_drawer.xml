<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
                android:id="@+id/nav_home"
                android:icon="@drawable/ic_action_home"
                android:title="@string/nav_text_home"/>
        <item
                android:id="@+id/nav_map"
                android:icon="@drawable/ic_action_map"
                android:title="@string/nav_text_map"/>
    </group>

    <item android:title="@string/nav_text_more">
        <menu>
            <item
                    android:id="@+id/nav_preferences"
                    android:icon="@drawable/ic_action_preferences"
                    android:title="@string/nav_text_preferences"/>
            <item
                    android:id="@+id/nav_about"
                    android:icon="@drawable/ic_action_info"
                    android:title="@string/nav_text_about"/>
        </menu>
    </item>

</menu>
